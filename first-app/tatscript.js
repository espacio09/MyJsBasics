// first function


function sayHello(name ) {
    console.log('Hello ' + name);
}

sayHello('Tatjana');

// -------------------------------------------------------
// global objects in Javascript
console.log(); // this function can be globally used

setTimeout();// standard js to be used on client, inside of browser or inside of node
clearTimeout();

setInterval();  //repeatedly call a function after a delay
clearInterval(), // stop that function from being called repeatedly

//------------------------------------------------------------

window.console.log  // window is a object that belongs to Javascript - automatically set before each function

// declare a variable

var message = '';
window


